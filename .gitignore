# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-electron
release-builds/
release-builds-v2/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Moltbook related files
moltbook_config.json
scripts/moltbook_*
skills/moltbook/
feed.json
status.json
register_payload.json
dm_check.json

# Local config
config.json
environment.json

# External sub-agent run outputs (subagent-orchestrator MCP)
.agent-runs/

# Scratch logs
git_log.txt
git_status.txt

.mcp.json

# Operator-local Claude permissions (shared defaults live in .claude/settings.json)
.claude/settings.local.json

# Operator-local Cursor MCP config (use .cursor/mcp.example.json)
.cursor/mcp.json

# Test coverage
coverage/
coverage_report*.txt
lint_results.txt
test_result.json
